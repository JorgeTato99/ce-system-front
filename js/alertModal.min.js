// ALERT MODAL

// inicialize container wrapper and append to body
let alertWrapper = document.createElement("div");
alertWrapper.id = "alertWrapper"
alertWrapper.classList.add("alertWrapper", "hidden")
alertWrapper.innerHTML = `<div class="alertModalWindow"></div>`
document.body.append(alertWrapper)

async function alertModal(messageText, btnOKText, btnCancelText){

	let alertModalWindow = alertWrapper.getElementsByClassName("alertModalWindow")[0]
	/// set Alert UI
	alertModalWindow.innerHTML = `
		<div class="alertModalMessage">${ messageText }</div>
		<div class="alertModalBtns">
			<button class="btnAlertOK">${ btnOKText }</button>
			<button class="btnAlertCancel">${ btnCancelText }</button>
		</div>`
	/// Maked visible
	alertWrapper.classList.remove("hidden")
	/// get modal elements
	let btnAlertOK = alertWrapper.getElementsByClassName("btnAlertOK")[0]
	let btnAlertCancel = alertWrapper.getElementsByClassName("btnAlertCancel")[0]

	/// Return slected value from btns eventListener
	return new Promise(resolve => {
		/// OK btn
		btnAlertOK.addEventListener("click", function(){
			alertWrapper.classList.add("hidden")
			resolve(true) // return value
		})
		/// Cancel bnt
		btnAlertCancel.addEventListener("click", function(){
			alertWrapper.classList.add("hidden")
			resolve(false) // return value
		})
	})
}

// console.log("alertModal.min.js")